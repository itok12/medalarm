Check the repo for release momentum:

1. Read `git status --short`
2. Look for the next concrete blocker to shipping MedAlarm
3. If a blocker is obvious, report it briefly
4. If nothing needs attention, reply `HEARTBEAT_OK`
